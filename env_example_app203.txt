# 养老社区改良改造看板 - 飞书登录与工作台免登
# 复制到 .env 或 .streamlit/secrets.toml 中配置

# 飞书应用（登录、多维表格/电子表格、Webhook 共用）
FEISHU_APP_ID=cli_xxx
FEISHU_APP_SECRET=xxx

# 默认表格链接（可选；侧栏默认值，也可用 FEISHU_TABLE_URL / FEISHU_BITABLE_URL）
# 电子表格示例：https://xxx.feishu.cn/sheets/{token}?sheet={子表id}
# 需在开放平台为应用开通 drive / sheets 等相关权限（只读或读写视需求）
# FEISHU_TABLE_URL=https://tkhome.feishu.cn/sheets/WHpesgmsohOVpLtdokocXotsnfe?sheet=dAFcmN

# 飞书登录门禁：设为 1 启用，未登录无法查看内容
FEISHU_LOGIN_REQUIRED=1

# 重定向 URL：应用完整地址，需与飞书「安全设置」->「重定向URL」一致
# 本地：http://localhost:8501/  Streamlit Cloud：https://xxx.streamlit.app/
FEISHU_REDIRECT_URI=http://localhost:8501/

# 飞书开放平台配置（登录功能）：
# 1. 安全设置 -> 重定向URL：添加 FEISHU_REDIRECT_URI 的值（如 https://xxx.streamlit.app/）
# 2. 权限管理 -> 开通「contact:user.base:readonly」（获取用户基本信息）
# 3. 网页应用 -> 添加 -> 在线网页 -> 桌面端主页：填授权链接（可选，工作台免登用）
#    格式：https://open.feishu.cn/open-apis/authen/v1/authorize?app_id=APPID&redirect_uri=ENCODED_REDIRECT_URI&scope=contact:user.base:readonly&state=default
#    redirect_uri 需 URL 编码，如 http%3A%2F%2Flocalhost%3A8501%2F

# ---------- 与 elderly-dashboard 共用的 MySQL（任选一种配置方式）----------
# 方式 A：完整连接串（可与 Postgres 等混用）
# APP203_DATABASE_URL=mysql+pymysql://用户:密码@主机:3306/数据库名?charset=utf8mb4

# 方式 B：分项变量（推荐在 Streamlit Secrets 里与另一仓库填相同值）
# MYSQL_HOST=xxx.rds.aliyuncs.com
# MYSQL_PORT=3306
# MYSQL_USER=app_user
# MYSQL_PASSWORD=your_password
# MYSQL_DATABASE=elderly_projects
# 未配置 MySQL 时仍使用本地 SQLite：APP203_DB_PATH=app203_projects.db
