# 复制本文件为 .env 后填写。与 elderly-dashboard-main 使用「完全相同」的数据库配置即可共用 projects 表。
# 优先级：APP203_DATABASE_URL > MYSQL_* > APP203_DB_PATH(SQLite)

# 推荐：一行连接串（两项目复制粘贴同一行）
# APP203_DATABASE_URL=mysql+pymysql://用户:密码@主机:3306/数据库名?charset=utf8mb4

# 或分项（两项目逐项填相同值）
# MYSQL_HOST=xxx.rds.aliyuncs.com
# MYSQL_PORT=3306
# MYSQL_USER=app_user
# MYSQL_PASSWORD=your_password
# MYSQL_DATABASE=elderly_projects

# 未配 MySQL 时回退本地 SQLite
# APP203_DB_PATH=app203_projects.db
